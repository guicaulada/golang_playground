package main // package main is the only one that generates an executable

import "fmt" // fmt is a shorthand for the word format, it's a standard package, you can find more about it on golang.org/pkg

func main() {
	fmt.Println("Hello World!")
}
